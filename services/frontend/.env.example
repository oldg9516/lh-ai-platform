# Backend API URL (FastAPI ai-engine)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# CopilotKit Runtime URL (AG-UI streaming endpoint)
# Uses Next.js API route by default: /api/copilot
NEXT_PUBLIC_COPILOT_RUNTIME_URL=/api/copilot

# Optional: Direct connection to FastAPI for AG-UI (alternative to Next.js proxy)
# NEXT_PUBLIC_COPILOT_RUNTIME_URL=http://localhost:8000/api/copilot

# Environment
NODE_ENV=development
