# LLM APIs
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Database (Supabase REST API)
# For local Docker Supabase:
SUPABASE_URL=http://supabase-api
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU
SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long
# For cloud Supabase (uncomment and replace):
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Pinecone
PINECONE_API_KEY=
PINECONE_INDEX=support-examples
PINECONE_DIMENSION=1024

# Langfuse (Observability) — auto-created on first boot, no need to change
LANGFUSE_PUBLIC_KEY=pk-lf-local
LANGFUSE_SECRET_KEY=sk-lf-local
LANGFUSE_HOST=http://langfuse-web:3000

# Encryption (AES-256-GCM for cancel links)
CANCEL_LINK_PASSWORD=

# App
DEBUG=false
LOG_LEVEL=INFO

# Mock APIs (set to false to use real external APIs)
USE_MOCK_APIS=true

# Real API credentials (optional, only needed if USE_MOCK_APIS=false)
# ZOHO_API_KEY=your_zoho_api_key
# ZOHO_CRM_URL=https://crm.zoho.com/crm/v2
# GOOGLE_MAPS_API_KEY=your_google_maps_key

# Chatwoot (Omnichannel)
CHATWOOT_URL=http://chatwoot-web:3010
CHATWOOT_API_TOKEN=
CHATWOOT_ACCOUNT_ID=1
CHATWOOT_ESCALATION_ASSIGNEE_ID=     # Optional: Agent ID to assign escalated conversations
CHATWOOT_SECRET_KEY_BASE=     # Generate with: openssl rand -hex 64
CHATWOOT_DB_PASSWORD=
CHATWOOT_REDIS_PASSWORD=
CHATWOOT_FRONTEND_URL=http://localhost:3010

# Phase 3+ (uncomment when needed)
# N8N_URL=https://n8n.diconsulting.pro
# N8N_API_KEY=

# Analytics Service (Phase 5)
ANALYTICS_DB_URL=postgresql://analytics_readonly:analytics_pass@supabase-db:5432/postgres

# Langfuse (Analytics Project) — create in UI: Settings → Projects → Create Project "Analytics Agent"
# These are SEPARATE from support AI Langfuse keys above
LANGFUSE_ANALYTICS_PUBLIC_KEY=pk-analytics-local
LANGFUSE_ANALYTICS_SECRET_KEY=sk-analytics-local

# Frontend (Phase 6) — only needed for Docker deployment
# In development mode (pnpm dev), the proxy automatically uses localhost:8000
# In Docker production, set to ai-engine service name
NEXT_PUBLIC_API_URL=http://ai-engine:8000
